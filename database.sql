ALTER TABLE users ADD COLUMN IF NOT EXISTS is_admin TINYINT(1) DEFAULT 0; ALTER TABLE users ADD COLUMN IF NOT EXISTS is_blocked TINYINT(1) DEFAULT 0; ALTER TABLE users ADD COLUMN IF NOT EXISTS blocked_until DATETIME NULL; CREATE TABLE IF NOT EXISTS api_calls_log (id INT AUTO_INCREMENT PRIMARY KEY, user_id INT NOT NULL, called_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE, INDEX idx_user_date (user_id, called_at));